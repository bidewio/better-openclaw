# better-openclaw

> CLI tool, REST API, and web UI for scaffolding production-ready OpenClaw stacks with Docker Compose. 186 services, 43 skill packs, one command.

## Project Description

better-openclaw generates complete Docker Compose stacks pre-wired with OpenClaw skills. Pick services, choose skill packs, and get a fully wired `docker-compose.yml`, `.env`, reverse proxy configs, and agent skill files.

## How to Use

1. **Web UI**: Visit https://better-openclaw.dev/new for the visual stack builder
2. **CLI**: Run `pnpm create better-openclaw@latest` or `npx create-better-openclaw`
3. **API**: POST to the generate endpoint with your stack configuration

## CLI Commands

```bash
# Interactive wizard (default)
pnpm create better-openclaw@latest [project-directory]

# Non-interactive options
pnpm create better-openclaw@latest --yes                    # Use defaults
pnpm create better-openclaw@latest --preset <name>          # Preset: minimal, creator, researcher, devops, full
pnpm create better-openclaw@latest --services <ids>         # Comma-separated service IDs
pnpm create better-openclaw@latest --skills <packs>         # Comma-separated skill pack IDs
pnpm create better-openclaw@latest --proxy <type>           # none, caddy, traefik
pnpm create better-openclaw@latest --domain <domain>        # Domain for auto-SSL
pnpm create better-openclaw@latest --monitoring             # Include Grafana + Prometheus
pnpm create better-openclaw@latest --gpu                    # Enable GPU for AI services
pnpm create better-openclaw@latest --dry-run                # Preview without writing files
pnpm create better-openclaw@latest --open                   # Open web UI in browser
```

## API Endpoints

Base URL (standalone API): `https://api.better-openclaw.dev/api/v1` (or configured API_URL)

| Method | Path | Description |
|--------|------|-------------|
| GET | /api/v1/health | Health check |
| GET | /api/v1/services | List all services (?category=, ?maturity=) |
| GET | /api/v1/skills | List skill packs (?services=id1,id2) |
| GET | /api/v1/presets | List preset stacks |
| POST | /api/v1/validate | Validate stack config before generation |
| POST | /api/v1/generate | Generate stack (returns files + metadata) |
| GET | /api/v1/openapi.json | Auto-generated OpenAPI 3.1 specification |
| GET | /api/v1/docs | Interactive Swagger UI documentation |

Web API (Next.js): `POST https://better-openclaw.dev/api/generate` — generates stack directly

### Generate Request Body

```json
{
  "projectName": "my-stack",
  "services": ["ollama", "qdrant", "n8n"],
  "skillPacks": ["ollama-local-llm", "qdrant-memory"],
  "proxy": "caddy",
  "domain": "example.local",
  "gpu": false,
  "monitoring": true
}
```

## Service Catalog Summary

36 categories, 186 services including: PostgreSQL, Redis, Neo4j, Qdrant, ChromaDB, Weaviate, n8n, Temporal, xyOps, FFmpeg, Remotion, MinIO, Caddy, Traefik, Grafana, Prometheus, Ollama, Whisper, Stable Diffusion, Open WebUI, LibreChat, Dify, AnythingLLM, Cal.com, Gitea, Outline, and many more.

Category examples: AI Coding Agents, AI Platforms & Chat UIs, AI / Local Models, Automation & Workflows, Vector Databases, Media & Video, Social Media, Analytics, Knowledge & Documents, Object Storage, Database & Caching, Developer Tools, Reverse Proxy, Monitoring, Browser Automation, Search, Communication, Desktop, and Streaming.

## Skill Packs

Pre-bundled OpenClaw skills: video-creator, research-agent, social-media, dev-ops, knowledge-base, local-ai, content-creator, ai-playground, coding-team, knowledge-hub.

## Key Features

- Traefik label generation with auto-SSL and HTTP-to-HTTPS redirect
- Distributed rate limiting with optional Redis backend
- Config migration utilities for forward compatibility
- Smart dependency resolution and conflict detection
- Bare-metal deployment support (hybrid native + Docker)
- Monitoring dashboards with Grafana + Prometheus

## Documentation

- Docs: https://better-openclaw.dev/docs
- API Reference: https://better-openclaw.dev/api-docs
- Swagger UI: /api/v1/docs (live interactive API documentation)
- Full AI-friendly docs: /llms-full.txt
