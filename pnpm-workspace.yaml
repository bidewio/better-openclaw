packages:
  - packages/*

# pnpm 10 defaults this to false, which silently resolves internal
# deps from the npm registry instead of the local workspace.
linkWorkspacePackages: true

onlyBuiltDependencies:
  - '@prisma/engines'
  - better-sqlite3
  - esbuild
  - prisma
  - sharp
